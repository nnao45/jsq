{
  "name": "jsq-browser-build",
  "version": "1.0.0",
  "private": true,
  "description": "JSQ browser bundle build scripts",
  "scripts": {
    "build": "webpack --config webpack.config.js",
    "build:watch": "webpack --config webpack.config.js --watch"
  }
}