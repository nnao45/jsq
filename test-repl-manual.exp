#!/usr/bin/expect -f

set timeout 10

spawn sh -c "echo '{\"name\": \"test\"}' | node dist/index.js"

expect "> "
send "$.name\r"

expect {
    -re "(.+)" {
        puts "Got output: $expect_out(1,string)"
    }
}

send "\x03"
expect eof